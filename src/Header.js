// src/Header.js

import React from 'react';

function Header({user, setView}) {
    return (
        <header className="App-header">
            <h1>FlowPomodor</h1>
            {/* Later buttons */}
        </header>
    );
} export default Header;